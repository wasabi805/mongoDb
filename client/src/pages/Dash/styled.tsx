import { styled, Box } from "@mui/material";

export const DashContainer = styled(Box)(() => {
  return `
        position: relative;
        height: 97vh;
        border: 2px solid purple;
        width: 100vw;
    
    `;
});
